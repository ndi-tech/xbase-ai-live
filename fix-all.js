const fs = require('fs');
const path = require('path');
const walkSync = (dir, files = []) => { const entries = fs.readdirSync(dir, { withFileTypes: true }); entries.forEach(entry => { const fullPath = path.join(dir, entry.name); if (entry.isDirectory()) walkSync(fullPath, files); else if (entry.name.endsWith('.tsx')) files.push(fullPath); }); return files; };
const fixEmojis = (content) => { const emojiMap = { '?????????':'??','????':'??','??????????':'??','????':'??','?????????':'??','????':'??','?????????':'??','????':'??','?????????':'??','????':'??','?????????':'??','????':'??','???????':'?','?':'?','??????????':'??','????':'??','??????????':'??','????':'??','?????????':'??','??':'??','??????':'?','?':'?','???????':'?','??????':'?','???????':'?','???????':'?','???????':'?','???????':'?','??????':'?' }; Object.keys(emojiMap).forEach(key => { content = content.replace(new RegExp(key, 'g'), emojiMap[key]); }); return content; };
const files = walkSync('app'); files.forEach(file => { let content = fs.readFileSync(file, 'utf8'); content = fixEmojis(content); fs.writeFileSync(file, content, 'utf8'); console.log('?', path.basename(file)); }); console.log('All files fixed!');
